#include <stdio.h>

int main(void){
	
	int n=;

	if (n>0) {
		printf("%i is positive\n", n);
	
	}
	else if (n=0) {
		printf("%i is zero\n", n);
	}

	else if (n<0) {
		printf("%d is negative\n", n);
	}

	return (0);
}
