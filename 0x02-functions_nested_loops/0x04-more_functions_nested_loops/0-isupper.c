#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * _isupper - checks for uppercase character
 * description: checks for uppercase character and returns 1 or 0
 * @c: character to be inputted
 * Return: 1 if true, 0 if otherwise
 */
int _isupper(int c)
{
	return (isupper(c));
}
